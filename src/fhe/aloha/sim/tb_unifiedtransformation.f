// Rung 3: tb_UnifiedTransformation (merged FFT/NTT engine).
// Validates FFTTw_RNS_ROM (NTT cfg) + FFTAllTwiddleROM (stored-FFT cfg).
-f $ALOHA_PORT/sim/aloha_engine.f
$ALOHA_SRC/Aloha-HE_Common/Testbench/tb_UnifiedTransformation.sv
